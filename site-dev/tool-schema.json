{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EnhanceClaude Tool Module",
  "description": "Schema for tool-info.json — defines how a skill or agent appears on enhanceclaude.com",
  "type": "object",
  "required": ["name", "type", "description", "os"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Display name of the skill or agent"
    },
    "type": {
      "type": "string",
      "enum": ["skill", "agent"],
      "description": "Whether this is a skill (instructions/knowledge) or an agent (autonomous behavior)"
    },
    "description": {
      "type": "string",
      "description": "Short summary shown in the module card"
    },
    "os": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["linux", "macos", "windows", "any"]
      },
      "description": "Compatible operating systems. Use 'any' if OS-agnostic. Enables automatic install instruction links."
    },
    "version": {
      "type": "string",
      "description": "Current version"
    },
    "author": {
      "type": "string",
      "description": "Author or contributor name"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Searchable tags for categorization"
    },
    "overview": {
      "type": "string",
      "description": "Detailed overview paragraph shown in the expanded detail view"
    },
    "customInstructions": {
      "type": "string",
      "description": "Special installation or usage instructions specific to this tool, if generic install instructions are not sufficient"
    },
    "promptSuggestions": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Example prompts the user can copy/paste to try this tool"
    },
    "applications": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Use cases"
    },
    "featured": {
      "type": "boolean",
      "default": false,
      "description": "Whether to highlight this module on the page"
    }
  },
  "additionalProperties": false
}
