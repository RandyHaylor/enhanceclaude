{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EnhanceClaude Tool Module",
  "description": "Schema for web-view.json — defines how a tool, skill, or agent appears on enhanceclaude.com",
  "type": "object",
  "required": ["name", "type", "description"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Display name of the tool, skill, or agent"
    },
    "type": {
      "type": "string",
      "enum": ["tool", "skill", "agent"],
      "description": "Module type"
    },
    "description": {
      "type": "string",
      "description": "Short summary shown in the module card"
    },
    "version": {
      "type": "string",
      "description": "Current version"
    },
    "author": {
      "type": "string",
      "description": "Author or contributor name"
    },
    "platforms": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Compatible platforms (e.g. Claude Code, ChatGPT, Gemini)"
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Searchable tags for categorization"
    },
    "readme": {
      "type": "string",
      "description": "Filename of the readme to render as module content (e.g. README.md)"
    },
    "repo": {
      "type": "string",
      "format": "uri",
      "description": "Source repository URL (not displayed to users, used for pulling content)"
    },
    "icon": {
      "type": "string",
      "description": "Filename of an icon image in the subfolder"
    },
    "featured": {
      "type": "boolean",
      "default": false,
      "description": "Whether to highlight this module on the page"
    }
  },
  "additionalProperties": false
}
