# shared-tmux-terminal

A Claude Code skill that opens a shared tmux terminal window where both Claude and the user can see and control the same session simultaneously. Claude sends commands and reads output autonomously; the user can type directly at any time, including for sudo passwords.

Supports Linux (gnome-terminal, konsole, alacritty, kitty, etc.) and macOS (Terminal.app via osascript).
